services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: kiddo
      POSTGRES_PASSWORD: kiddo
      POSTGRES_DB: kiddo_week
    ports:
      - "5432:5432"
    volumes:
      - kiddo_week_db:/var/lib/postgresql/data

volumes:
  kiddo_week_db:
